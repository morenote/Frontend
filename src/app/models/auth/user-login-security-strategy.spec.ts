import { UserLoginSecurityStrategy } from './user-login-security-strategy';

describe('UserLoginSecurityStrategy', () => {
  it('should create an instance', () => {
    expect(new UserLoginSecurityStrategy()).toBeTruthy();
  });
});
