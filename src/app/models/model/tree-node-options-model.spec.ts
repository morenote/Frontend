import { TreeNodeOptionsModel } from './tree-node-options-model';

describe('TreeNodeOptionsModel', () => {
  it('should create an instance', () => {
    expect(new TreeNodeOptionsModel()).toBeTruthy();
  });
});
