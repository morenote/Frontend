import { SecurityConfigDTO } from './security-config-dto';

describe('SecurityConfigDTO', () => {
  it('should create an instance', () => {
    expect(new SecurityConfigDTO()).toBeTruthy();
  });
});
